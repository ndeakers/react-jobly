import React from "react";
import { useHistory } from "react-router-dom";
/**
 * /profile
 * 
 * props: handleSave fn, currentUser
 * state: formData
 * */

function ProfileForm() {

  return (<h1>ProfileForm!</h1>)
}

export default ProfileForm;